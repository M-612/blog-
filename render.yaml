services:
  - type: web
    name: simple-blog-fullstack
    env: node
    buildCommand: |
      cd frontend
      npm install
      npm run build
      cd ../backend
      npm install
    startCommand: cd backend && npm start
